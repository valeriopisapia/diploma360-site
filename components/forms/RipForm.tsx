import { LeadForm } from './LeadForm'

/** Ripetizioni lead form — thin LeadForm wrapper, see LeadForm.tsx for behavior. */
export function RipForm() {
  return <LeadForm origine="ripetizioni" prodotto="Ripetizioni" showPerChi />
}
